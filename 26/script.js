let repeticao = ["repetição", "repetição", "repetição", "repetição", "repetição"]

repeticao.forEach(element => {
    let resultado = document.getElementById("resultado");
    resultado.innerHTML += element + "     ";
});


